export default function SettingsPlanPage() {
  return (
    <div className="space-y-2">
      <h3 className="text-base font-semibold text-gray-900">Plan & Pricing</h3>
      <p className="text-sm text-gray-500">
        Billing/plan management endpoints are not present in the backend API documentation yet, so this page is disabled.
      </p>
    </div>
  );
}

export default function RecruiterSettingsPlanPage() {
  return (
    <div className="space-y-4">
      <div>
        <h2 className="text-base font-bold text-gray-900">Plan & Pricing</h2>
        <p className="text-sm text-gray-500 mt-1">
          Billing/plan management endpoints are not present in the backend API documentation yet, so this page is disabled.
        </p>
      </div>
    </div>
  );
}
